# ls aliases
alias ll='ls -l'
alias la='ls -lA'
alias l='ls -CF'

# git alias
alias gs='git status'

# kubernetes alias
alias kb='kubectl'

# terraform alias
alias tf='terraform'
alias tfp='terraform plan'
alias tff='terraform fmt -recursive'
